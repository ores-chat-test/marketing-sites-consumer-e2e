{
  "name": "@ores-chat-test/marketing-sites-consumer-e2e",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Live GitHub Pages consumer fixture for the production ORES Chat marketing component.",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "npm test"
  },
  "engines": {
    "node": ">=22"
  }
}
